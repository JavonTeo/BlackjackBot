module.exports = {
    name: 'allcommands',
    description: 'lists all available commands the bot can handle',
    execute(message)
    {
        message.channel.send('list of commands not complete');
    }
};